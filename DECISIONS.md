# 决策记录

## 2026-07-01：做完整问卷 onboarding（逆调研结论，但有约束）

调研（见 research/break-apps-settings-onboarding.md）显示：这个品类几乎没人做上来问卷式个性化，都是“好默认 + 事后调 + 行为自适应”。

仍决定做完整问卷，原因：

- 产品差异点 + 自媒体素材价值（“会问你问题、懂你的灵狐”很出片）。
- 开发者本人是首个用户，想亲历这个流程。

约束（把调研教训用上）：

- 只问 5 题，每题都真实映射到设置/画像，不问废话。
- 全程可“跳过用默认”。
- 动作推荐用加权随机（软偏好），不硬筛，保留多样性。
- 下一步补“行为自适应”（按跳过/完成动态调权重）——那才是真正对的个性化路径。

## 2026-07-01：前台 App 检测不申请任何权限

调用 get-windows 时传 `{ accessibilityPermission: false, screenRecordingPermission: false }`。

原因：

- 我们只需要「前台是哪个 App」（app 名称，走 NSWorkspace，本就不需要权限），不需要窗口标题。
- 不传这两个参数时，get-windows 的辅助程序（独立可执行文件 `node_modules/get-windows/main`）会每次调用都弹一次辅助功能授权框；而且它和 App 主体是不同的可执行文件，用户给「健身助手」授的权它不共享，于是**授了权还一直弹**。
- 关掉后：零权限即可用，不再弹框，体验反而更好。

## 2026-06-30（晚）：核心机制改为「等 AI 回复的间隙」触发

原来的提醒触发是番茄钟式固定计时。现在改成：**检测到你在编码工具里停手、大概率正在等 AI 回复时**才弹提醒。

原因：

- 这是和 stretchly / 普通番茄钟最本质的差异——别人定时打断你，我们只在你本来就空闲时出现，不破坏心流。
- 非常贴合目标人群（vibe coding），也极适合做自媒体内容。
- 你一敲键盘提醒立刻收起，绝不打断打字。

代价：读取前台 App 需要 macOS 辅助功能权限；没权限时降级为久坐 + 喝水兜底提醒。

## 2026-06-30（晚）：技术栈从 Tauri 改为 Electron

之前 03-tech-plan 选 Tauri。实际落地改为 **Electron + 原生 JS/HTML**。

原因：

- 「等 AI」机制要读系统空闲时间和前台 App，Electron 用 `powerMonitor` + `get-windows` 直接拿；Tauri 要写 Rust。
- 最成熟的久坐提醒开源项目 stretchly 是 Electron + JS + BSD-2 协议（可商用闭源），同栈才能直接借鉴，不用全用 Rust 重写。
- 开发者本人熟 TS/Web，上手快，能尽快自用验证。

代价：包体更大、后台稍重。长期若要更轻，可再评估迁移。

## 2026-06-30：第一版做桌面宠物，不做完整健身 App

原因：

- 健身 App 已经很多，完整训练、饮食、体重记录会很快变大。
- 这个项目最鲜明的场景是“人在电脑前坐太久”，应该先吃透桌面场景。
- 桌宠可以把提醒从冷冰冰通知变成陪伴式反馈，更贴合 vibe coding 人群。

## 2026-06-30：先自建办公室微运动动作库

原因：

- GitHub 上成熟健身项目有很多 GPL/AGPL 许可证，商业闭源产品直接复制会带来风险。
- MVP 不需要 1000 个动作，只需要 20-50 个适合办公室的安全小动作。
- 先用原创 seed 数据跑通体验，再决定是否接入外部 API 或开源数据。

## 2026-06-30：技术首选 Tauri + TypeScript

原因：

- 适合做轻量桌面小窗、通知、托盘、常驻进程。
- 相比 Electron 更轻，长期后台运行更合适。
- 前端仍可用熟悉的 Web 技术做宠物状态和动画。

